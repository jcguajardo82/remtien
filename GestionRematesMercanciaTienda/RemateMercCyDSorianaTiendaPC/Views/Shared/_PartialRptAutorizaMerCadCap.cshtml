@model IEnumerable<EDReciboTiendaSoriana.EDReportes.EDRMerCadCap>
@{
    var grid = new WebGrid(Model, canPage: false, canSort: false);
    var columns = new List<WebGridColumn>();
}
@grid.GetHtml(
                                tableStyle: "table table-striped table-bordered table-hover dataTables-example gvv",
                                columns:

                                    grid.Columns(


                                                
                                                grid.Column("CodigoBarra", "Código"),
                                                grid.Column("Descripcion", "Descripción"),
                                                grid.Column("Lote", "Lote"),
                                                grid.Column("FechaCaducidad", "Fecha Caducidad"),
                                                grid.Column("CantPiezas", "Cantidad Sugerida"),
                                                grid.Column(columnName: "",
                                                    header: "Confirmar",
                                                    format: (item) => @Html.Raw("<input type='checkbox' class='box' checked='true' />")
                                                            ),
                                        @*grid.Column(header: "Cantidad Capturada"
                                                        , format: @<text>
                                            <input id="idcantidad" name="cantidad" type="text" class="form-control required">
                                                        </text>),*@

                                                   grid.Column(header: "Cantidad Capturada", format: @<span>@Html.TextBox("idcantidad", @item.CantPiezas as object, new { @class = "form-control"})</span>)
                                                    )



                            )

    @*string html = grid.GetHtml(columns: columns).ToString();
    @MvcHtmlString.Create(@html.Replace("{Confirmar}", "<input type='checkbox' id='allBox'/>"));*@


